The figure shows a client sending mail.  The message reaches the email server and the server decides if the message is for one of its clients.  If it is not, then it forwards the message to another email server for delivery.