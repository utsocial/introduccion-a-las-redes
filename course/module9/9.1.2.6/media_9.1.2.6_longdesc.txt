Figure shows a computer with three different applications open and the port number associated with each.  Below each application is the specific protocol in use and the transport layer adds the proper port number to form the segment.  POPv3 uses port 110, HTTP uses port 80 and Instant Messaging uses port 531.