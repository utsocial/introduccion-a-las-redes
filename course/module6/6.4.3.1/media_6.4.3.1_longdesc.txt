Figure 1 shows a router with two LANs.  Each LAN has 2 PCs.  This figure shows how a host can communicate with another host on the same LAN without the use of the gateway. Figure 2 shows a router with two LANs.  Each LAN has 2 PCs . This figure shows how a host requires the gateway to communicate with a host on another LAN.